{
    "version": 2,
    "builds": [
        {
            "src": "server.ts",
            "use": "@vercel/node",
            "config": {
                "command": "node server.ts"
            }
        }
    ],
    "routes": []
}